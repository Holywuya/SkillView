##调试模式
DEBUG: false

# 基础玩家伤害值（用于技能伤害计算）
BasePlayerDamage: 100.0

# 数据库配置
Database:
  # false = 使用本地 SQLite (data.db)
  # true = 使用下方 MySQL 配置
  enable: false

  # MySQL 配置
  host: localhost
  port: 3306
  user: root
  password: ""
  database: minecraft

  # 表名
  table: player_skills_kv
